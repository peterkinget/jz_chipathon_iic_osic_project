echo 'Reading User Preferences in /foss/designs/.designinit'

alias ls='ls --color=auto'
eval "$(dircolors -b /foss/designs/user_setup/dircolors-solarized/dircolors.ansi-dark )"

tree() {
  dir="${1:-.}"
  find "$dir" -print | sed -e 's;[^/]*/;|____;g;s;____|; |;g'
}

export PATH="/foss/designs/user_setup/bin:$PATH"

cd /foss/designs
